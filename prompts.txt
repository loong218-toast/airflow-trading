Instructions: Treat the current code as hyperparameter optimizer that grid search for ml training. It handle large amount of data so the system is designed to avoid crash by being memory efficient. Dont be defensive/fallback so we can avoid silent killer in the code. The params are stored inside run_config.json for easy tweak so dont put hardcoded features/metrics in the code. It is time series data for trading. Don't drop rows with NaN that will cause calc problems in time series calc. Minimal use of try except. Don't use .copy() or at least minimal. the code are polars based. The system in running docker compose airflow with Linux.

The original data is 5m data. All calc should only used for close price.

the final result master columns are, Columns: ['balance', 'SL', 'TP', 'win_pos', 'total_pos', 'side', 'exit_window_h', 'era_int', 'config_id', 'ma_int', ma_price_gap, ma_price_gap_a, ma_price_gap_b, ma_price_gap_c, 'ma_reversion', 'entry_lookback_h', max_dd]

The point of the grid search is loop and find all combos with variables listed in run_config. SLTP, and calculate each entry_lookback_h with each position. The MAs and Stoch should be precomputed by global or era because they don't change and same with each market, and be cached for future to speed up the grid search. Entry lookback is condition that depends on SLTP values.

The final result is million of aggregated rows, balance is percentage and mean(), max_dd is maximum drawdown(mean) and should filter out too much drawdown which is a variable and to avoid compute. There is also a equity table which will be generated to record all trades and their time series equity curve data.

Each SLTP is unique by era_int(time_windows)

ma_int is bitwise logic, store Boolean logic up to four MAs, from 0 to 0000 to 1111. 
ma_price_gap_h is the gap between price and selected gap which is variable from run_config. ma_price_gap a is the gap between least period ma to next higher period ma. gap_b and gap_c is gap between higher period ma and highest period ma.  When the ma_int is true for the specific ma, filtering so that only price above the specific ma calculated/exist as buy example. When ma reversion is on, it calc the inverse of that ma logic used. MA is trend following logic. Ma gap calc should result in positive or negative based on how gaps interact. All aggregated ma gaps are median.

era_int is each window used for that grid search, total pos is total position, exit_window_h is how long the position hold after exit by hours, entry_lookback_h is filter out the price that is higher(for buy) or lower(for sell) than current price by hour so trend following. 

Make sure everything align with final columns. Any calc compute only happens when it is not zero and true for a ma, entry_lookback stoch for example, because they need to be enabled in run_config.

All different combos are saved as cache for future


remember to keep the MA nan if calc involved the nan rows
Keep in mind since the data is 2017-2025 + 2025-2026, there is a empty data hole in between around 2025 oct to 2026 march. The system should just put balance as NaN and skip to next part, and should be nan proof. Becasue in ml training, i will drop those cols with nan MA to avoid MA inaccuracy


